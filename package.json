{
  "name": "bank-files-harmonizer",
  "version": "0.1.0",
  "private": true,
  "description": "Agent de revue fiscale pre-declaratif pour harmoniser les fichiers comptables et detecter les anomalies TVA/RAS.",
  "scripts": {
    "api:build": "docker compose build api",
    "api:compile": "docker compose run --rm -e PYTHONPATH=. api uv run --extra dev python -m compileall app tests scripts",
    "api:db:migrate": "docker compose run --rm -e PYTHONPATH=. api uv run --extra dev alembic upgrade head",
    "api:dev": "docker compose up api",
    "api:lint": "docker compose run --rm -e PYTHONPATH=. api uv run --extra dev ruff check app tests scripts",
    "api:smoke:llm": "docker compose run --rm -e PYTHONPATH=. api uv run python -m scripts.smoke_llm_provider",
    "api:test": "docker compose run --rm -e PYTHONPATH=. -e DATABASE_URL= api uv run --extra dev pytest -q",
    "api:typecheck": "docker compose run --rm -e PYTHONPATH=. api uv run --extra dev mypy app tests scripts",
    "compose:down": "docker compose down",
    "compose:up": "docker compose up",
    "secrets:check": "git ls-files -z | xargs -0 python3 api/scripts/secret_scanner.py"
  },
  "engines": {
    "node": ">=22",
    "npm": ">=10"
  }
}
